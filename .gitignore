.claude/worktrees/
.venv/
__pycache__/
*.egg-info/
.pytest_cache/

# Derived from the sibling evidence corpus by `memoria normalize`; not
# committed, same as the raw evidence corpus it derives from. Regenerate
# with `memoria normalize` (see README).
sources/normalized/
