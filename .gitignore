.claude/worktrees/
.venv/
__pycache__/
*.egg-info/
.pytest_cache/

# Derived from the evidence corpus; not committed, same as the raw evidence
# it derives from. Nothing regenerates these today -- the normalizer was
# written for the retired Thoreau corpus (docs/open-problems.md 2.4) and no
# replacement is chosen. Ignored still, so that records restored by hand or
# by a future normalizer are not committed by accident.
sources/normalized/
sources/editorial/

# FTS5 search index; derived state, not durable (§42). Regenerate with
# `memoria rebuild` (see README).
.memoria/
